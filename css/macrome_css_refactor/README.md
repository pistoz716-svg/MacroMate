# MacroMe CSS Refactor

Copy every file inside this `css/` folder into:

D:\APPS\APPS\MacroMate\css\

Keep this line in `index.html`:

<link rel="stylesheet" href="css/style.css" />

`style.css` now imports the modular CSS files.
